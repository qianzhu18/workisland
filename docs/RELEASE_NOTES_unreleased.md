# Release Notes（待发布）

状态：`待发布` · 未打 Tag、未对外发布。关联 Change：`2026-08-21-idle-island-visibility`（[PRD-009](./product/prd/PRD-009-IDLE-ISLAND-VISIBILITY-FIX.md)）。

## Island 显示模式：极简 / 常驻

- 设置页中原「通知模式」「没有会话时隐藏」两个否定式开关收敛为一个正向选择：**Island 显示模式**。
  - **常驻（新装默认，2026-08-22 产品决策）**：空闲时保留顶部紧凑胶囊；普通后台运行不会长期展开面板。新装默认常驻是为了首次启动即可感知安装成功（配合默认开启的启动提示音），避免"下载完什么都不显示"的困惑。
  - **极简**：没有待办事项或重要提醒时，Island 不占用可见顶部空间；仍可通过顶部透明热区、菜单或快捷键（默认 `Ctrl+I`）唤回。觉得常驻打扰的用户可随时在「设置 → Island 行为」切换。
- 两种模式下：任务提交与完成短暂显示（时长由「完成通知停留时间」控制）；审批、提问和错误持续显示，不会被自动隐藏计时器收起。
- 首次启动欢迎页会说明 Island 常驻位置、启动提示音，以及如何切换到极简模式。
- 旧版本中「通知模式」「没有会话时隐藏」偏好会在首次启动新版本时一次性迁移（详见 Change `evidence.md` 迁移映射）；已显式选择常驻胶囊的用户保持常驻，之后显示行为只由新模式字段决定。已迁移到极简的既有安装不受新装默认影响。
