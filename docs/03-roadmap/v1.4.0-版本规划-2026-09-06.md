# v1.4.0 版本规划（2026-09-06 定稿）

> 本文是 v1.4.0 的范围基线（SSOT）：改范围先改这里再动手。认领任何条目前，先看 [GitHub Milestone v1.4.0](https://github.com/qianzhu18/workisland/milestone/1) 与文末的流转纪律。

## 主题：覆盖与留存 · 第一阶段收口

v1.3.0 完成了公测门槛（Agent Doctor / 反馈通道 / 首启引导 / 签名公证）。v1.4.0 把竞品对标中已完成的"留存与开放"特性**发出去**，并补上发布侧的中英双语能力——重点是**交付**，不是开新摊子。

## 范围修订（2026-09-07）

- 用户拍板：**会话搜索与回源跳转**为下一版本核心功能——issue [#115](https://github.com/qianzhu18/workisland/issues/115)（已入 Milestone v1.4.0，`kind: epic` / P0），完整方案 PRD-019 随本修订入库 `docs/product/prd/`。
- 发版纪律：**#115 不阻塞上面已就绪四功能的 tag**——M1–M3 全绿赶上就作为本版头牌发布；赶不上则搜索部分顺延 v1.5.0（届时把 issue 迁出 milestone 并回写本节）。
- 交付线：M1 索引器（PR #120，`feature/session-search`）→ M2 搜索 UI + ⌘K + `SESSION_SEARCH_*` IPC + 回源跳转 → M3 设置开关 + 一键清除索引。
- SSH/tmux 远程接入拆出为独立 epic [#116](https://github.com/qianzhu18/workisland/issues/116)，blocked 于 ADR-0005 裁定，明确不进 v1.4。

## 一、已定内容（代码已合 main，本版发布）

| 功能 | PR | 说明 |
| --- | --- | --- |
| 安静时段 + 锁屏静音 | #95 | 设置 → 声音；Bark 推送刻意不受抑制 |
| Plan 确认卡 Markdown 渲染 | #92 | 折叠展开，复用岛上既有渲染样式 |
| 本地开发者 API | #93 | `127.0.0.1:9938`，默认关闭，Bearer 鉴权 |
| 用量发现通道（首批） | #94 | zcode / opencode / claude 三数据源，不依赖 transcript_path |
| 远程接入 observe-only 第一阶段 | #122 | issue #116 第一阶段：SSH 隧道状态上岛，设置 → Agents → 远程主机；指南 docs/REMOTE_ONBOARDING.md |
| 全应用中英双语 | #138 | issue #110 已关闭：跟随系统 / 简体中文 / English、实时切换与词条质量门 |

> 范围修订 2026-09-07：负责人裁定 ADR-0005 第一阶段（observe-only）通过，#122 并入 v1.4.0 候选范围；tmux attach 等交互式能力属二期，另裁后开工（`feature/remote-pairing-ui` 保留）。2026-09-09：#138 合入 `main` 并关闭 #110；中英双语以完整检查和真实 macOS 验收为前提纳入本候选。

## 二、发布必做（chore，不完成不 tag）

- [ ] **Release Notes 中英双语**（#107，milestone 内）：v1.4.0 起 Release 页中英双语，CHANGELOG 每版配英文摘要
- [ ] CHANGELOG 与 Release Notes 补录本候选的全部六项用户能力
- [ ] 官网 changelog 同步
- [ ] **tag 后验证 x64 管线**：#103 修复了 Intel 构建路径，本版 tag 是第一个验证点——x64 DMG 若产出并过签名公证，宣发才解锁"支持 Intel"

## 三、择机（时间允许则进，否则顺延）

- [ ] **性能口碑数据**（#108，milestone 内）：闲置 CPU < 3% 实测达标即可上 README/官网；不达标则只出内部报告与优化计划

## 四、明确不进 v1.4（v1.5+ 候选，勿提前开工）

| 条目 | Issue | 状态 | 前置 |
| --- | --- | --- | --- |
| 无刘海/内置屏悬浮条 | #109 | needs-prd | 形态 PRD（附件形态，复用 PR #36 设计，勿重蹈 #28→#43） |
| 会话/tab 级聚焦抑制 | #111 | needs-validation | 终端焦点信号能力矩阵 |
| 五大终端 tab 级跳转 | #112 | needs-validation | 逐终端实机验证 |

## 五、平行线：Windows 复活（独立版本线，不占 v1.4.0）

- 版本策略：`win-v0.1.0-alpha.x` 独立 prerelease 线（与 Mac v1.x 互不干扰），发 alpha 包给真实 Windows 用户验证
- 解冻三条件：① CI 全绿（已达成）② ≥1 名真实 Windows 用户核心链路验证留证 ③ 一名 Windows 维护者持续贡献
- **同学认领入口**：飞书任务表「windows 端兼容」卡 → 从建「Windows alpha 发包与实机验证」issue 开始，PR 附实机证据

## 六、分工

| 角色 | 负责 |
| --- | --- |
| 张锦鸿（产品） | 范围拍板、PRD 裁定（#109/#110）、安全边界裁定（ssh/ADR-0005） |
| ZCode 会话（研发） | 实现与发版、issue/PR 纪律维护、飞书看板同步 |
| 同学（Windows 线） | 按第五节认领，实机验证留证 |

## 七、流转纪律（所有协作者共用）

1. **需求池**（飞书）= 想法与证据，未排期不动工
2. **GitHub Issue**（打 `status: ready`）= 可认领的交付待办；PR 描述写 `Closes #x`，合并自动销账
3. **规划文档**（本文件）= 版本范围基线，改范围先改这里
4. 完成后飞书任务卡回写状态；"已交付"口径 = 用户手上能用（发版为准），代码合 main 不算
